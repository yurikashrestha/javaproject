package yurikaassign;
/*Declare two double variables, length and width,
and assign them values 5.5 and 3.0, respectively.
 Calculate the area (length * width) and print the result./*
 */

public class uri {
    public static void main(String[] args){
        double length = 5.5;
        double width = 3.0;
        double area = (length*width);
        System.out.println("The area is " + area);
    }
}
